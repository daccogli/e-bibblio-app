{
  "name": "e-biblio-app",
  "version": "1.0.0",
  "description": "Library management system",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run client\" \"npm run server\"",
    "client": "(cd client-app && npm start)",
    "server": "(cd server-app && npm start) ",
    "install": "(cd server-app && npm install);(cd client-app && npm install); (cd shared-library && npm install); ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "node-sass": "^4.14.1",
    "redux-thunk": "^2.3.0",
    "winston": "^3.3.3"
  }
}
